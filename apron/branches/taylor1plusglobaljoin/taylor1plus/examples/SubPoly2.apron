/* i==j if (y == 0) */

var x : real, y : real, i : real, j : real;
begin

assume i <= 10 and i >= 0;
assume j <= 10 and j >= 0;

x = i;
y = j;
if (x<=0) then halt;
endif;

while (x > 0) do
    x = x - 1;
    y = y - 1;
done;

if (y == 0) then
skip;
endif;

end

